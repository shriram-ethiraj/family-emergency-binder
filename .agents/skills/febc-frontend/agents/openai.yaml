interface:
  display_name: "FEBC Frontend"
  short_description: "Build and review the binder creator UI"
  default_prompt: "Use $febc-frontend to implement this frontend change."
policy:
  allow_implicit_invocation: true
